import { GenerefilterPipe } from './generefilter.pipe';

describe('GenerefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
